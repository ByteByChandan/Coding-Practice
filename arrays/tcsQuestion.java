import java.util.*;
class operation{
    public int returnElement(int []arr){
        int []jrr=arr;
        Arrays.sort(jrr);
        int z=0;
        int []yrr=new int[z];
        for(int i=0;i<arr.length;i++){
           // if(arr[i]=)
        }
        return 0;
    }
}
class tcsQuestion {
    public static void main(String []args){
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter your Array: ");
        int s=sc.nextInt();
        int []arr=new int[s];
        for(int i =0;i<arr.length;i++){
            arr[i]=sc.nextInt();

        }
        operation obj = new operation();
    }
}
